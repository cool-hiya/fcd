extends ../popup/popup.pug 

include ../input/input.pug
include ../arrow-button/arrow-button.pug
include ../toxin-check/toxin-check.pug

block header
    p.title._h1 Войти

block content
    .signin-popup
        form
            +input({class:'signin-popup__field', type: 'email', placeholder: 'Email'})
            +input({class:'signin-popup__field', type: 'password', placeholder: 'Пароль'})
            +arrow-button({text: 'Войти'})
        +toxin-check({action: 'Создать', href: '#'})
